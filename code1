    // Get sales for a prescription
    function getSales(uint256 prescriptionId)
        external
        view
        onlyAuthorizedPharmacist
        returns (Sale[] memory)
    {
        require(prescriptions[prescriptionId].id != 0, "Prescription does not exist");
        return sales[prescriptionId];
    }
}
